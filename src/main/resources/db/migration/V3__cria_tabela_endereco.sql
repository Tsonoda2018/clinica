create table endereco (
       id integer auto_increment ,
        cidade varchar(255),
        numero varchar(255),
        rua varchar(255),
        pessoa_id integer,
        primary key (id)
    )